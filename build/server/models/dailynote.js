// Generated by CoffeeScript 1.9.3
var DailyNote, cozydb;

cozydb = require('cozydb');

module.exports = DailyNote = cozydb.getModel('DailyNote', {
  date: Date,
  content: String
});

DailyNote.all = function(callback) {
  return DailyNote.request('byDate', callback);
};

DailyNote.getByDate = function(date, callback) {
  return DailyNote.request('byDate', {
    key: date
  }, function(err, notes) {
    if (err) {
      return callback(err);
    }
    return callback(null, notes[0]);
  });
};
